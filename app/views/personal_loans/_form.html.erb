<%= form_with(model: personal_loan, local: true) do |form| %>
  <% if personal_loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(personal_loan.errors.count, "error") %> prohibited this personal_loan from being saved:</h2>

      <ul>
      <% personal_loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :purpose %>
    <%= form.text_field :purpose %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
